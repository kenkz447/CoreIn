@model ActionViewModel
@{
    if (Model is ActionViewModel)
    {
        if (Model.ViewLayout != null)
        {
            Layout = Model.ViewLayout;
        }
        else
        {
            Layout = "_Layout";
        }
    }
}